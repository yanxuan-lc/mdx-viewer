# BACKLOG

The active index: **`queued` / `in-progress` only**. It is read at the start of every session
that records an idea, so it has to stay near-free — agent-written `unconfirmed` entries live
in `backlog/INBOX.md` and never land here.

| id | title | type | severity | source | status | footprint | relations | evidence |
|---|---|---|---|---|---|---|---|---|
