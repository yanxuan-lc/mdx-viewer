The same import line as `tier-b.mdx`, but inert inside a fenced code block —
documenting JavaScript is a primary use case and must not be implicated.

```js
import Thing from "./does-not-exist.js"
```
